Create A game widnow using Raylib
Window resolution is yours 
Configure the window before creating it
where should 
FLAG_WINDOW_RESIZABLE for Allows the user to resize the window. (can't work now but need when we add setting for resoltion)
FLAG_MSAA_4X_HINT for Turns on 4× anti-aliasing if the GPU supports it.
FLAG_VSYNC_HINT Synchronizes rendering with your monitor's refresh rate to reduce screen tearing.

function BeginDrawing() for start to draw in side window 
function EndDrawing(); for end to draw in side window

every thing draws between this 2 mean all our frame draw between this 2 function

Write a code that clear window and create backgraound black
Show any message in window can be any postion and size and color 
Show Frame rate 
